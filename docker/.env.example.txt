# --- Mongo (dev) ---
MONGO_INITDB_ROOT_USERNAME=dev
MONGO_INITDB_ROOT_PASSWORD=devpassword

# --- Backend ---
PORT=4000
PARTY_TOKEN_SECRET=dev-secret
ALLOW_ORIGIN=http://localhost:3000

# --- LLM (Ollama) ---
# Pour dev rapide, commence avec un modèle léger
OLLAMA_MODEL=llama3:8b
